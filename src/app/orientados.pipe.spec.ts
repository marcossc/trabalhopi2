import { OrientadosPipe } from './orientados.pipe';

describe('OrientadosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrientadosPipe();
    expect(pipe).toBeTruthy();
  });
});
